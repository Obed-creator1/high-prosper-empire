# High Prosper Services - Skeleton

Contains backend (Django) and frontend (Next.js) skeleton with a single Customer table.

See backend/requirements.txt and frontend/package.json

## Setup

### Backend
```bash
cd backend
python -m venv .venv
source .venv/bin/activate  # On Windows: .venv\Scripts\activate
pip install -r requirements.txt